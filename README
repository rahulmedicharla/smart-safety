This project is a mobile application that provides campus-focused safety features for OSU students.

# Features

-> Uses Firebases' realtime database to provide crowd sourced crime alerts

-> Uses historical data about crime & k-means clustering to identify "hot crime zones" and identifies your safety level based on your location and time of day.

-> Developed a safety focused routing algorithm that provides the safest navigation route based on real time alerts.

# How to run

Mobile Application: (cd app && npm i && expo start)

Backend api: (cd backend && python api/main.py)
